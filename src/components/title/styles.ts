import styled from "styled-components";

export const StyledTitle = styled.h2`
  font-size: 1.25em;
  color: white;
  font-weight: 800;
  font-family: "Sono";
  letter-spacing: -0.5px;

  margin-top: -1.5vh;
`;
